# TraceForge telemetry contract

Shared semantic attributes and correlation headers are documented in `docs/ARCHITECTURE.md`.
Python instrumentation lives beside the orchestrator and demo target to keep the first vertical
slice executable without a separate package build.

